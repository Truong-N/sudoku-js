const merge = (unsolved, eliminated) => {
  const mergeArr = [...unsolved];
  for (let r = 0; r < 9; r++) {
    for (let c = 0; c < 9; c++) {
      for (let z = 0; z < 9; z++) {}
    }
  }
};
// start
// unsolved = [1,2,3]
// eliminated = unsolved = mergeArr
// if in pencil=true click eliminate = 0
// in merge if unsolved[i] != eliminated[i] then mergeArr[i]= eliminate
